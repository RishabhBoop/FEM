def alpha1(x,y):
  if (x<=dd and abs(y)<=Hm/2.) or (x<=Bm-dd and Hm/2.-dd<=y<=Hm/2) or (x<=Bm-dd and -Hm/2.<=y<=-Hm/2+dd):
    val=mu_1
    plt.plot(x,y,'go')
  elif Bm-dd<=x<=Bm and abs(y)<=Hm/2:
    val=mu_2
    plt.plot(x,y,'bo')
  else:
    val=1
  return 1./(mu_0*val*x)

def alpha2(x,y):
  return alpha1(x,y)


def beta(x,y):
  val=0
  return val


def rho(x,y):
  if dd+lls<=x<=dd+lls+sd and -SH<=y<=SH:
    j0=I1/(sd*2*SH)
    plt.plot(x,y,'ro')
  elif Bm-dd-lls-sd<=x<=Bm-dd-lls and abs(y)<SH/2.:
    j0=-I2/(sd*SH)
    plt.plot(x,y,'ro')
  elif Bm+lls<=x<=Bm+lls+sd and abs(y)<SH/2.:
    j0=I2/(sd*SH)
    plt.plot(x,y,'ro') 
  elif abs(x-rm0)<=ma/2. and abs(y-zm0)<=mb/2.:
    j0=12.*M0/ma*( (2*(x-rm0)/ma)**5 )*(1-(2*(y-zm0)/mb)**6)
    #plt.plot(x,y,'yo')    
  else:
    j0=0
  return j0  

def robin_g(x,y):
  val=1./mu_0/x/np.sqrt(x**2+y**2)
  return val

def robin_q(x,y):
  val=0
  return val

def dirichlet_p(x,y):
  val=0
  return val



  ####################
  #
  # Auswertung e)
  #
  ####################

  rr=p[rAchse,0]
  psi=u[rAchse]
  
  rm=rr[:-1]+np.diff(rr)/2.
  Bz=np.diff(psi)/np.diff(rr)/rm
  Hz=Bz*1.

  for i,x in enumerate(rm): 
    if x<=dd or Bm-dd<=x<=Bm:  
      Hz[i]/=mu_0*mu_1 



  ####################
  #
  # Auswertung f)
  #
  ####################
  rr=p[rAchse,0]
  psi=u[rAchse]
  
  rm=rr[:-1]+np.diff(rr)/2.
  Bz=np.diff(psi)/np.diff(rr)/rm
  Hz=0*Bz
  for i,x in enumerate(rm):
    if rm0-ma/2<=x<=rm0+ma/2:
      Hz[i]=Bz[i]/mu_0-M0*(1-(2*(x-rm0)/ma)**6)
    elif x<=dd:  
      Hz[i]=Bz[i]/(mu_0*mu_1)
    elif Bm-dd<=x<=Bm: 
      Hz[i]=Bz[i]/(mu_0*mu_2)
    else:
      Hz[i]=Bz[i]/mu_0

       